import math as mt # import as name can be changed
print(mt.factorial(int(input("Enter the number: "))))
# print(mt.factorial(int(input("Enter the number: ")))) "this will not work"

from math import sqrt
from maths import sqrt as gaurav


